# Travista — Frontend

This is the Next.js frontend for the Travista project.

## Quick start

Prerequisites:

- Node.js 18+ (or the version used by the team)
- pnpm / npm / yarn

Install dependencies and run the dev server:

```bash
# install
npm install
# start dev server
npm run dev
```
